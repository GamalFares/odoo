services:
  - type: web
    name: faris-jewelry-odoo
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
      # SECURITY: Set these in Render Dashboard Environment tab
      # DO NOT put credentials here in production
